input() → waits for the user to type something and press Enter.
Each answer is stored in a variable (name, age, hobby).

The program then combines text + variables to create a message.

\n adds a blank line before the greeting (for better readability).
